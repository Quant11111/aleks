name: Deploy Portfolio

on:
  push:
    branches: ["main"]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Create logs directory
        run: mkdir -p logs

      - name: Restart PM2 aleks instance
        run: pm2 restart aleks
